# Shared reusable workflow for GitHub Actions security analysis.
#
# Other moov-io repos should call this rather than vendoring zizmor-action:
#
#   name: zizmor
#   on:
#     push:
#     pull_request:
#     schedule:
#       - cron: '17 6 * * 1'
#     workflow_dispatch:
#   permissions: {}
#   jobs:
#     zizmor:
#       permissions:
#         security-events: write
#         contents: read
#         actions: read
#       uses: moov-io/.github/.github/workflows/zizmor.yml@master
#
# Findings are uploaded to GitHub code scanning and do not fail the job.
# Use https://app.stepsecurity.io/ to improve workflow security
name: zizmor

on:
  push:
    branches: [master]
  pull_request:
  schedule:
    - cron: '17 6 * * 1'
  workflow_call:
  workflow_dispatch:

permissions: {}

jobs:
  zizmor:
    name: zizmor
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run zizmor
        continue-on-error: true
        uses: zizmorcore/zizmor-action@3dc1ecc9bcb9e94e9b2c709687979e1298497054 # v0.6.2
        with:
          # Findings go to code scanning and do not fail zizmor itself.
          # continue-on-error covers GitHub code-scanning upload 503s.
          advanced-security: true
          fail-on-no-inputs: false
